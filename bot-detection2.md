- ## Community Features
 این حساب عضو چه گروهی است و ارتباطش با اعضای گروه چگونه است؟
 مثلا در ویژگی تعداد فالوور با هم کامینتی هستند یا چند ویژگی

 - ## Graph-Based Methods
   - **Follow/Follower Analyze**
     - همه این حساب‌ها فقط همدیگر را فالو کرده‌اند و تقریباً هیچ ارتباطی با کاربران واقعی ندارند.
    
    - **Random Walk**
      - مدل به‌صورت تصادفی روی شبکه حرکت می‌کند تا ببیند کدام کاربران معمولاً کنار هم دیده می‌شوند. از یوزر A حرکت میکنه و میبینه در حرکت بین فالورها و یا هر ویژگی مدام به حلقه ABC میخوره بنابراین یک خوشه پیدا کرده.

    - **MRF ( Markov Random Field )**
      - اگر دو همسایه User X ربات باشند، احتمال ربات بودن User X نیز افزایش پیدا می‌کند.
    
    - **Belief Propagation** 
      - اگر یک حساب مشکوک باشد، مقداری از این مشکوک بودن به حساب‌های متصل به آن منتقل می‌شود. یعنی اگر ساید یک کاربر را با درصد بالا بدانیم از ارتباط او میتوان درصدی همین ساید را به اطراف داد.
    
    - **Trust-based Detection**
      - کاربران واقعی معمولاً به کاربران واقعی اعتماد بیشتری دارند. در واقع هیچ کاربر واقعی ارتباط انسانی و زیادی با یک ربات ندارد
    
    - **Node Embedding (DeepWalk / Node2Vec)**
      -  ویژگی های هر کاربر (فالور، فالونیگو ریتویئت، منشن و...  ) به یک بردار عددی تبدیل می شوند که ساختار شبکه اطراف او را مشخص میکنه اگر دو کاربر بردارهای مشابه هم داشته باشند احتمالا در یک ساید قرار دارند.
   -  **Community Detection ( Bot2Vec )**
       - به‌جای بررسی یک کاربر، رفتار کل گروه بررسی می‌شود. اگر یک حساب جدید وارد کامیونیتی 1 شود و رفتارش شبیه اعضای آن باشد، احتمال زیادی وجود دارد که آن نیز ربات باشد.
 


 - ## GNN ( Graph Neural Network ) Methods
    - **GCN (Graph Convolutional Network)**
        - اطلاعات همسایه‌ها را با اطلاعات خود گره ترکیب می‌کند. برای تشخیص User B، مدل فقط خود B را نگاه نمی‌کند؛ بلکه ویژگی‌های Bot A و Bot C را نیز با ویژگی‌های B ترکیب می‌کند.
 اگر بیشتر همسایه‌های B ربات باشند، احتمال ربات بودن B افزایش پیدا می‌کند.
    - **RGCN** (Relational GCN)
        - نسخه توسعه‌یافته GCN که نوع رابطه بین گره‌ها را نیز در نظر می‌گیرد (مثلاً Follow و Follower). GCN همه این ارتباط‌ها را تقریباً یکسان می‌بیند. اما RGCN به هر کدام وزن می دهد
    - **GAT (Graph Attention Network)**
        - همه همسایه‌ها اهمیت یکسان ندارند؛ مدل خودش یاد می‌گیرد به کدام همسایه بیشتر توجه کند. همسایه‌های مهم‌تر تأثیر بیشتری روی تصمیم دارند.

    - **GraphSAGE**
      - به‌جای بررسی همه همسایه‌ها، فقط تعدادی از آن‌ها را نمونه‌گیری می‌کند. مثلا بجای هزار همسایه فقط 100 همسایه مورد بررسی قرار میگیرد این نمونه گیری هم روش هایی دارد.
        - Uniform Random Sampling
        - Weighted Sampling
        - Importance Sampling
        - Layer-wise Sampling
    - **DropEdge**
      - هنگام آموزش، بعضی ارتباط‌ها را موقت حذف می‌کند. مدل مجبور می‌شود فقط به چند ارتباط خاص وابسته نباشد. در نتیجه مدل بهتر روی داده‌های جدید عمل می‌کند و بیش‌برازش کاهش می‌یابد.
    
    - **SATAR ( Self-supervised Approach to Twitter Account Representation Learning )** 
      - فرض کنید یک حساب توییتر داریم که **متن توییت‌هایش طبیعی است، اما بیشتر با حساب‌های ربات در ارتباط است و الگوی پروفایلش نیز مشکوک است**. **SATAR** این سه نوع اطلاعات (**متن، ویژگی‌های پروفایل و ارتباط‌های شبکه**) را هم‌زمان بررسی می‌کند و با استفاده از **Attention** یاد می‌گیرد کدام‌یک برای این حساب مهم‌تر است؛ مثلاً ممکن است نتیجه بگیرد که در این مثال، **ارتباط‌های شبکه از متن مهم‌تر هستند**، بنابراین وزن بیشتری به آن‌ها بدهد و در نهایت با ترکیب این اطلاعات تصمیم بگیرد که احتمال ربات بودن حساب زیاد است. هدف اصلی SATAR این است که به‌جای تکیه بر یک ویژگی، از چندین نوع اطلاعات به‌صورت هوشمند برای ساخت یک نمایش دقیق از هر حساب استفاده کند.

- ## Feature Fusion
 فرض کنید سه نوع اطلاعات از کاربر داریم (اطلاعات پروفایل، کامیونیتی، متن توئیت ها) به چند روش میتونیم این اطلاعات را به مدل بدهیم.

  - **Concatenation**
    - ویژگی‌ها را بدون تغییر، پشت سر هم قرار می‌دهد.

  - **Summation**
    - ویژگی‌های هم‌اندازه را با هم جمع می‌کند.

  - **Average / Max Pooling**
    - با میانگین یا بیشترین مقدار، ویژگی‌ها را به یک بردار خلاصه تبدیل می‌کند.

  - **Gated Fusion**
    - مدل یاد می‌گیرد کدام ویژگی مهم‌تر است و به همان وزن بیشتری می‌دهد.

  - **Self-Attention Fusion**
    - هر ویژگی با سایر بخش‌های همان ویژگی ارتباط برقرار می‌کند تا اطلاعات مهم‌تر مشخص شوند.

  - **Cross-Attention Fusion**
    - هر نوع ویژگی با ویژگی‌های دیگر ارتباط برقرار می‌کند تا وابستگی بین آن‌ها یاد گرفته شود. *(روش استفاده‌شده در BotCF)*

  - **Co-Attention**
    - دو نوع ویژگی به‌صورت دوطرفه هم‌زمان به یکدیگر توجه می‌کنند.

  -  **Smart Fusion**
      - که برای هر اطلاعات وزن بدیم که مدل درجه اهمیت را بفهمد

 - ## Attention
  - **Attention:** مدل هنگام تصمیم‌گیری یاد می‌گیرد به مهم‌ترین اطلاعات توجه بیشتری کند؛ مثلاً در جمله «گربه روی مبل خوابیده است»، برای فهم کلمه «خوابیده» بیشتر به «گربه» توجه می‌کند تا «روی».

  - **Self-Attention:** مدل ارتباط اجزای یک مجموعه را با خود همان مجموعه بررسی می‌کند؛ مثلاً در یک توییت، کلمات همان توییت به یکدیگر توجه می‌کنند تا معنای متن بهتر درک شود.

  - **Cross-Attention:** مدل ارتباط بین دو مجموعه متفاوت را یاد می‌گیرد؛ مثلاً ویژگی‌های **Community** به متن توییت توجه می‌کنند تا مشخص شود آیا محتوای حساب با رفتار گروهی آن سازگار است یا خیر.
